# Figma / React design reference

This tree is the original UI kit export (React + shadcn components). **Godot does not load anything from here at runtime.**

For the in-game UI, edit:

- `theme/maze_wars_colors.gd`
- `textures/ui/*.svg`
- `scripts/ui/components/mw_*.gd`
